---
import Layout from '../layouts/Layout.astro';
import Test from "../components/Test.svelte"
import db from "../db"


const projects = await db.query.project.findMany({
  with: {
    tasks: true,
  },
});

---


<Layout>
<Test client:visible/>
<code><pre>{JSON.stringify(projects, null, 2)}</pre></code>
</Layout>
